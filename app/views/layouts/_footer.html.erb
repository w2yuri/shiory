<footer class="py-2" style="background-color: #d7e6d5;">
  <div class='container text-center'>
    <small>&copy; 2024 Shiory
    <% unless admin_signed_in? || customer_signed_in? %>
       / 
      <%= link_to '管理者ログイン', admin_session_path %>
    <% end %>
    </small>
  </div>
</footer>