<%= form_with(model:[post, comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'5',placeholder: "コメントを入力して下さい", class: "w-100 comment-textarea" %>
  <%= f.submit "送信する", class: "btn btn-primary", style: "color: #ffe9a5;" %>
<% end %>