<div class='container'>
  <div class='row mt-5'>
    <h2>Follower Users</h2>
    <% if @customers.exists? %>
      <%= render 'public/customers/index', customers: @customers %>
    <% else %>
      <p>ユーザーはいません</p>
    <% end %>
  </diV>
</diV>