<table class='table'>
  <tr><%= image_tag user.get_profile_image(100, 100), class: "rounded-circle" %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
  <!--<tr>-->
  <!--  <th>follows</th>-->
  <!--  <th><#%= link_to user.followings.count, user_followings_path(user) %></th>-->
  <!--</tr>-->
  <!--<tr>-->
  <!--  <th>followers</th>-->
  <!--  <th><%#= link_to user.followers.count, user_followers_path(user) %></th>-->
  <!--</tr>-->
</table>
　
<div class='row'>
  <!--ログインしているユーザーが現在表示されているユーザーと等しくない場合-->
  <% if current_customer != user %>
    <td><%#= render "relationships/btn", user: user %></td>
  <% else %>
    <%= link_to edit_public_user_path(user), class: "btn btn-outline-secondary btn-block edit_user_#{user.id}" do %>
    Edit
    <%= link_to "Show", public_user_path(user:id), class: "btn btn-outline-secondary btn-block" %>
  <% end %>
  <% end %>
</div>