<table>
  <tbody>
    コメント数: <%= travel_task.travel_task_comments.count %>
      <% travel_task.travel_task_comments.each do |comment| %>
      <tr>

        <td>
          <%= link_to customer_path(comment.customer) do %>
            <%= image_tag comment.customer.get_profile_image(50, 50), class: "rounded-circle" %><br>
            <%= comment.customer.name %>
          <% end %>
        </td>
        <td><%= comment.content %></td>
        <td>
          <% if comment.customer == current_customer %>
            <%= link_to "Destroy", post_travel_task_comment_path(travel_task.post.id, comment), method: :delete, class: "btn btn-danger pull-right", remote: true %>
          <% end %>
        </td>
      </tr>
      <% end %>
  </tbody>
</table>